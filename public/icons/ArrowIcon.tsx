import {IconProps} from "../../types";

const ArrowDownIcon = (props?: IconProps) => (
    <svg
        viewBox="0 0 16 16"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        {...props}
    >
        <path d="M8 11 4 6h8l-4 5z" fill="currentColor" />
    </svg>
);
export default ArrowDownIcon;
